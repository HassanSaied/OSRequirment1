CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
PROJECT(OSRequirment)
set (CMAKE_CXX_STANDARD 11)
SET(IncludedFiles Includes/defs.h Includes/headers.h Includes/ProcessStruct.h Includes/clkUtilities.h Includes/queueUtilities.h)
SET (ProcessGeneratorSourceFiles Src/processGenerator.cpp)
SET(ProcessSourceFiles Src/process.cpp)
SET(SchedulerSourceFiles Src/scheduler.cpp)
SET(ClockSourceFiles Src/clk.cpp)
SET(FilesGeneratorSourceFiles Src/FilesGenerator.cpp)
add_executable(Clock ${IncludedFiles} ${ClockSourceFiles})
add_executable(processGenerator ${IncludedFiles} ${ProcessGeneratorSourceFiles})
add_executable(process ${IncludedFiles} ${ProcessSourceFiles})
add_executable(scheduler ${IncludedFiles} ${SchedulerSourceFiles})
add_executable(filesGenerator ${IncludedFiles} ${FilesGeneratorSourceFiles})
include_directories(${CMAKE_ROOT_DIRECTORY} Includes)